const helloArr = ['Hello World!'];

module.exports = helloArr;
module.exports = {
  moduleOne
};

function moduleOne(x, y) {
  const lo = 17;
  const test = `test in ${lo}`;
  console.log(test);

  return x * y;
}